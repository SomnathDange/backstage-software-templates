apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.app_name }}
  description: Python app that that displays time - Named ${{ values.app_name }}
  annotations:
    github.com/project-slug: somanath-dange-backstage/${{ values.app_name }}
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: ${{ values.app_name }}
    sonarqube.org/project-key: ${{ values.sonar_org }}/${{ values.app_name }}
spec:
  type: service
  owner: development
  lifecycle: experimental
